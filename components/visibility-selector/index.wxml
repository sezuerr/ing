<view class="selector">
  <view wx:for="{{options}}" wx:key="value" class="option {{value === item.value ? 'active' : ''}}" data-value="{{item.value}}" catchtap="select">
    {{item.label}}
  </view>
</view>
